{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "pnpm: dev",
            "command": "pnpm run dev",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$vite"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}